import { TarifFiltrePipe } from './tarif-filtre.pipe';

describe('TarifFiltrePipe', () => {
  it('create an instance', () => {
    const pipe = new TarifFiltrePipe();
    expect(pipe).toBeTruthy();
  });
});
